[build]
buildCommand = "npm run build"

[deploy]
startCommand = "node serve.js"
healthcheckPath = "/health"
healthcheckTimeout = 30

# Set API_URL to the URL of your server Railway service, e.g.:
# API_URL = "https://your-server.up.railway.app"
